# --- third-party binaries (fetch these yourself; see README "Prerequisites") -
# Oracle JDBC is OTN-licensed and not redistributed here.
hop/lib/*.jar

# --- runtime state ----------------------------------------------------------
# Extract output, regenerated by migrate.cmd.
data/*.csv
data/*.txt
data/manifest.json
data/bulk-expectations.json

# logs
*.log
*.err

# --- editor / OS / agent ----------------------------------------------------
.claude/
.idea/
.vscode/
*.iml
Thumbs.db
desktop.ini

# keep empty scaffolding directories
!**/.gitkeep
